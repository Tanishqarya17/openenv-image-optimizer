# openenv-image-optimizer
An OpenEnv RL environment where an LLM agent acts as an MLOps engineer. It dynamically sequences image operations to rescue corrupted data, maximizing classifier accuracy to automate ML pipelines.
